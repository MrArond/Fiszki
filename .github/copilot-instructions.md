# Copilot Instructions

## Project Guidelines
- The user communicates in Polish and is building a .NET MAUI mobile application connected to an ASP.NET Core API. The app uses SecureStorage for auth tokens.